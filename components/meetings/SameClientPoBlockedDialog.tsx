import { Modal, Pressable } from 'react-native';
import { Text, YStack } from 'tamagui';
import { useBizlinkColors, BIZLINK_FONTS } from '../../lib/theme';
import { BizButton } from '../bizlink/BizButton';
interface Props { visible: boolean; onViewPo: () => void; onContinue: () => void; onCancel: () => void; }
export function SameClientPoBlockedDialog({ visible, onViewPo, onContinue, onCancel }: Props) { const colors = useBizlinkColors(); return <Modal visible={visible} transparent animationType="fade" onRequestClose={onCancel}><Pressable onPress={onCancel} style={{ flex: 1, backgroundColor: 'rgba(0,0,0,0.5)', alignItems: 'center', justifyContent: 'center', padding: 24 }}><Pressable onPress={(event) => event.stopPropagation()} accessible={false}><YStack accessible accessibilityRole="alert" accessibilityViewIsModal backgroundColor={colors.card} borderRadius={24} padding="$4.5" width={320}><Text fontSize={18} fontFamily={BIZLINK_FONTS.semibold} color={colors.orange}>PO already active</Text><Text fontSize={13} fontFamily={BIZLINK_FONTS.medium} color={colors.muted} marginTop="$2" lineHeight={19}>This client already has an active or approved PO for the current cycle.</Text><YStack gap="$2" marginTop="$4"><BizButton label="View PO" variant="white" onPress={onViewPo} /><BizButton label="Continue meeting without new PO" onPress={onContinue} /><BizButton label="Cancel" variant="white" onPress={onCancel} /></YStack></YStack></Pressable></Pressable></Modal>; }
