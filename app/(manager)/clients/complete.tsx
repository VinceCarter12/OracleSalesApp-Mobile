// F-205: reuses app/(tabs)/clients/complete.tsx as-is (Complete Info) — a
// manager's own prospect needs the same checklist-completion flow an agent's
// does.
export { default } from '../../(tabs)/clients/complete';
