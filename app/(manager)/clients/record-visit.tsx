// F-205: New/Existing Manager-owned clients use the same Sales fast path.
// `useClientFlowRoutes()` resolves subsequent navigation back into the
// `(manager)/clients` route group.
export { default } from '../../(tabs)/meetings/record-visit';
